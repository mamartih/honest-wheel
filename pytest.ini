[pytest]
addopts = --strict-markers

markers =
    network: hits Alpaca's real paper API through the official CLI binary.
        Skips itself when the binary or the dedicated credentials are not in
        the environment. Run everything except it with: pytest -m "not network"
